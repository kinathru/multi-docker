sudo: required
services:
  - docker

before_install:
  - docker build -t kinath/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true kinath/react-test npm test

after_success:
  - docker build -t kinath/multi-client ./client
  - docker build -t kinath/multi-nginx ./nginx
  - docker build -t kinath/multi-server ./server
  - docker build -t kinath/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push kinath/multi-client
  - docker push kinath/multi-nginx
  - docker push kinath/multi-server
  - docker push kinath/multi-worker